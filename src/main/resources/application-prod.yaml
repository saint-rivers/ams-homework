spring:
  datasource:
    url: jdbc:postgresql://db.ams.com:5432/ams
    username: amsadmin
    password: ams123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

files:
  storage:
    path: /opt/resources/files

server:
#  name: https://springams.hrd-edu.info
  name: http://8.219.139.67:6654

logging:
  file:
    name: ${LOG_FILE:/opt/ams-kshrd.log}
